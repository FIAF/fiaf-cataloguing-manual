% config.tex

\usepackage[a4paper, margin=1in]{geometry}

\usepackage{fontspec}
\setmainfont[Mapping=tex-text,AutoFakeSlant=0.2]{BrownStd}
\setmonofont{Fira Code}

\usepackage{hyperref}
% \hypersetup{
%     colorlinks=true,   % Enable colored links
%     linkcolor=orange,    % Set link color
%     urlcolor=orange,      % Set URL color
%     citecolor=orange    % Set citation color
% }

\usepackage{xcolor}
\hypersetup{
    colorlinks=true,
    linkcolor=orange, % Custom color for internal links (namerefs)
    urlcolor=teal     % Custom color for external links
}

\usepackage{footmisc}    % Load footmisc package for more control over footnotes
\setlength{\footnotemargin}{0pt}  % Removes any left margin for footnotes

\usepackage{tcolorbox}
\tcbset{colback=gray!5!white, colframe=gray!75!black, title=Example}

\usepackage{tabularx,ragged2e}
\newcolumntype{L}{>{\RaggedRight}X}

\usepackage{colortbl}

\usepackage{enumitem}
\newlist{tabitemize}{itemize}{1}
\setlist[tabitemize,1]{label=\textbullet, left=0pt, nosep,
                       before={\begin{minipage}[t]{\linewidth}},
                       after ={\end{minipage}}}

\usepackage{xltabular}
