
\documentclass{article} 

\usepackage{hyperref} % library for hyperlinks
\usepackage{longtable} % library for tables
\usepackage{fontspec} % library for fonts
\setmainfont{BrownStd} % define specific font

% Set paragraph indent to zero
\setlength{\parindent}{0pt}

% Ensure empty lines between paragraphs result in full paragraph breaks
\setlength{\parskip}{\baselineskip}

\begin{document}
$body$
\end{document}