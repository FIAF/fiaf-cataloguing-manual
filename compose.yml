services:
  mkdocs:
    image: python:3.12-slim
    container_name: manual-mkdocs
    ports:
      - 8000:8000
    volumes:
      - .:/app
    working_dir: /app
    command: bash -c "pip install -r requirements.txt && mkdocs serve --dev-addr 0.0.0.0:8000"
